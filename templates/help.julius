$(document).ready(function(){
  $('#status-syntax-help').hide().attr('title','ステータス書式説明').dialog({
    autoOpen: false,
    width: 500,
    modal: false
  });
  $('#button-status-syntax-help').click(function(e){
    $('#status-syntax-help').dialog('open');
    e.preventDefault();
  });
});
